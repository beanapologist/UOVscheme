name: Python

on:
  push:
    branches: [main]
    paths:
      - 'impl/python/**'
      - '.github/workflows/python.yml'
  pull_request:
    branches: [main]
    paths:
      - 'impl/python/**'
      - '.github/workflows/python.yml'

jobs:
  test:
    name: pytest / Python ${{ matrix.python-version }}
    runs-on: ubuntu-latest

    strategy:
      fail-fast: false
      matrix:
        python-version: ['3.9', '3.11', '3.12']

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip
          cache-dependency-path: impl/python/requirements-dev.txt

      - name: Install test dependencies
        run: pip install -r impl/python/requirements-dev.txt

      - name: Run tests
        working-directory: impl/python
        run: |
          python -m pytest tests/ -v \
            --tb=short \
            --cov=uov \
            --cov=statecert \
            --cov-report=term-missing \
            --cov-fail-under=90

      - name: Run demos (smoke tests)
        working-directory: impl/python
        run: |
          python examples/demo.py
          python demos/silentverify_demo.py

  lint:
    name: Lint (ruff)
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: pip
          cache-dependency-path: impl/python/requirements-dev.txt

      - name: Install ruff
        run: pip install ruff

      - name: Lint with ruff
        working-directory: impl/python
        run: ruff check uov/ statecert/ tests/ examples/ demos/

      - name: Format check
        working-directory: impl/python
        run: ruff format --check uov/ statecert/ tests/ examples/ demos/
